import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';import {Box3,Vector3} from '../dist/three.module.js';import {GLTFLoader} from '../dist/vendor/loaders/GLTFLoader.js';
test('all eight clothing v2 exports contain one correctly sized character and portable materials',async()=>{for(const t of ['P','L','N','S','G','B','R','K']){const b=await readFile(new URL('../dist/assets/stages/yankee/'+t+'.glb',import.meta.url));const gltf=await new GLTFLoader().parseAsync(b.buffer.slice(b.byteOffset,b.byteOffset+b.length),'');const roots=[];gltf.scene.traverse(o=>{if(o.name.startsWith('Unit_'))roots.push(o);assert(!o.isCamera&&!o.isLight);});assert.equal(roots.length,1);assert.match(roots[0].name,/ClothingV2/);const box=new Box3().setFromObject(gltf.scene);const size=box.getSize(new Vector3());assert(size.x<3&&size.z<3&&size.y>1.5&&size.y<2.3,t);assert(box.min.y>-.1&&box.min.y<.1,t);let materials=new Set();gltf.scene.traverse(o=>{if(o.isMesh)materials.add(o.material.roughness);});assert(materials.size>2,'cloth/leather/metal differences preserved');}});
