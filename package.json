{
  "name": "aether-shogi",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:web": "playwright test",
    "build": "node scripts/validate-assets.mjs",
    "catalog": "node scripts/build-presentation-catalog.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "three": "0.170.0"
  }
}
